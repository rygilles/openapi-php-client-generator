<?php

{{ namespace is defined ? "namespace " ~ namespace ~ ";" : "" }}

{% if uses is defined %}
{% for use in uses %}
use {{ use }};
{% endfor %}
{% endif %}

class {{ className }} {{ extends is defined ? "extends " ~ extends : "" }}
{
{% if operations is defined %}
{% for op in operations %}
    public function {{ op.operationId }}({% if op.parameters is defined %}{% include 'includes/functionDefinitionParameters.php.twig' with {'parameters': op.parameters} %} {%  endif %})
    {

    }
{% endfor %}
{% endif %}
}
